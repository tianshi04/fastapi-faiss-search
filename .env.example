# .env.example
# This file contains example environment variables.
# Copy this file to .env and fill in your own values for local development.

# --- FAISS & Data Configuration ---

# The dimension of the vectors your model produces.
# This MUST match your embedding model's output.
VECTOR_DIMENSION=128

# The directory where the index and ID map files will be stored.
DATA_DIR=data


# --- API Server Configuration ---

# The host address for the API server. 0.0.0.0 makes it accessible on your local network.
API_HOST=localhost

# The port for the API server.
API_PORT=8000


# --- Search Logic Configuration ---

# A normalization factor to convert FAISS L2 distance to a confidence score (0-1).
# For L2-normalized vectors, the max distance is 2.0.
# Formula: confidence = max(0, 1.0 - (distance / NORMALIZATION_FACTOR))
DISTANCE_NORMALIZATION_FACTOR=2.0